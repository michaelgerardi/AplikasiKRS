<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplikasikrs">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tambah_Dosen"></activity>
        <activity android:name=".DosenActivity" />
        <activity android:name=".EditKelolaKRSActivity" />
        <activity android:name=".DataDiriActivity" />
        <activity android:name=".KelolaKRSActivity" />
        <activity android:name=".EditMatakuliahActivity" />
        <activity android:name=".EditMahasiswa" />
        <activity android:name=".MatakuliahActivity" />
        <activity android:name=".CrudMahasiswa" />
        <activity android:name=".EditDosenActivity" />
        <activity android:name=".Cruddosen" />
        <activity android:name=".DaftarIsiActivity" />
        <activity android:name=".SplasshScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>